// This file re-exports the Vercel Analytics component for use in Expo/React Native environments.
// In a web-only project, you would use: import { Analytics } from '@vercel/analytics/react';
// For Expo/React Native, analytics is not natively supported, but we include this for web builds.

export { Analytics } from '@vercel/analytics/react';
